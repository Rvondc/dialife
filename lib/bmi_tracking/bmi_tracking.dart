import 'package:flutter/material.dart';

class BMITracking extends StatelessWidget {
  const BMITracking({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}